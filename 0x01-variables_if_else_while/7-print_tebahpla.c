#include <stdio.h>

/**
 * main - print lower case alphabet in reverse
 *
 * Return: always 0
 */
int main(void)
{
	int s = 0;

	for (; s < 10; S++)
	{
		printf("%d", s);
	}
	putchar('\n');
	return (0);
}
